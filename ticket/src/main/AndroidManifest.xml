<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.ticket.test.login"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.RESTART_PACKAGES" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserMenuActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ToBuyActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ToQueryActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ToReturnActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ViewOrederActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SummitOrderActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SelectQueryActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".QuerybyDistrictActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".AdminMenuActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".DetailActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".InsertfilmActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".DeleteMovieActivity"
            android:label="@string/app_name" />

        <activity android:name=".SelectSeatActivity"
            android:label="@string/app_name"/>
    </application>

</manifest>